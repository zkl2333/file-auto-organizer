import { describe, it, expect } from 'vitest';

describe('Health API', () => {
  it('应该通过健康检查', () => {
    // 这是一个简单的健康检查测试
    // 实际 API 测试需要启动 Next.js 服务器，这里只做基础验证
    expect(true).toBe(true);
  });
});
